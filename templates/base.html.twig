<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Miru{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" type="text/css" href="../../css/bootstrap.css">
            <link rel="stylesheet" type="text/css" href="../../css/style.css">
        {% endblock %}
    </head>

    <header>
        <nav id="menu">
            <ul id="burger">
                <li><a id="tabs" href="{{ path('app_list_index') }}">Accueil</a></li>
            </ul>
            <div id="headerdisconnect">
                {% if not app.user %}
                {% else %}
                    <p>Bonjour {{ app.user.username }} (<a href="{{ path('app_login_logout') }}">se déconnecter</a>)</p>
                {% endif %}
            </div>
            <ul id="burger">
                <li><a id="tabs" href="{{ path('app_login_login') }}">Connexion</a></li>
                <li><a id="tabs" href="{{ path('app_signin_index') }}">Inscription</a></li>
            </ul>
        </nav>
    </header>

    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
